The program was written in February 2011.

It uses a K-way merge algorithm to count words in a big file that may be too 
large for loading into memory. A word is just a sequence of characters and, 
theriotically, all words may be unique.